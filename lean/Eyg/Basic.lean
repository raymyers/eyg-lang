import Cslib

/-!
# EYG formalization — starting point

This module just checks that the `cslib` dependency is wired up correctly.
Replace the placeholder below with the real EYG formalization.
-/

namespace Eyg

/-- Smoke-test theorem to confirm the toolchain builds. -/
theorem hello : 1 + 1 = 2 := by rfl

end Eyg
